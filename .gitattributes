# Ground-truth extracts from the `eurocontrol` R package are large binary parquet.
# They MUST go through git-lfs — see reference/README.md.
reference/**/*.parquet filter=lfs diff=lfs merge=lfs -text
